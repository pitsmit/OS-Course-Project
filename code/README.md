# cp Dockerfile /media/pitsmit/MYFLASH

#Эта структура идентифицирует запросы транзакций USB. URB-ы должны быть выделены вызовом usb_alloc_urb() и освобождаться вызовом usb_free_urb(). 
#Инициализация может быть выполнена различными функциями usb_fill_*_urb(). URB-ы предоставляются с помощью usb_submit_urb(), и ожидающие выполнения 
#запросы могут быть отменены с помощью usb_unlink_urb() или usb_kill_urb().


#Сектор 0: Загрузочный сектор (MBR/VBR)
#Сектор X: FAT1 (первая копия FAT таблицы)
#Сектор Y: FAT2 (вторая копия, backup)
#Сектор Z: Корневой каталог
#Сектор N: Область данных

# lsblk
# sudo mkfs.vfat -F 32 -n MYFLASH /dev/sda1